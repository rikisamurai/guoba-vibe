name: React Doctor

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/qr-codes/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  react-doctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: millionco/react-doctor@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          diff: main
          directory: apps/qr-codes
